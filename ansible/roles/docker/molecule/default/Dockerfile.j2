# Molecule managed dockerfile for docker role testing
FROM {{ item.image }}

ENV DEBIAN_FRONTEND=noninteractive
ENV container=docker

# Install systemd and required packages
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        systemd \
        systemd-sysv \
        python3 \
        python3-apt \
        sudo \
        curl \
        ca-certificates \
        gnupg \
    && rm -rf /var/lib/apt/lists/* \
    && rm -f /lib/systemd/system/multi-user.target.wants/* \
    && rm -f /etc/systemd/system/*.wants/* \
    && rm -f /lib/systemd/system/local-fs.target.wants/* \
    && rm -f /lib/systemd/system/sockets.target.wants/*udev* \
    && rm -f /lib/systemd/system/sockets.target.wants/*initctl* \
    && rm -f /lib/systemd/system/basic.target.wants/* \
    && rm -f /lib/systemd/system/anaconda.target.wants/*

# Create test user that the role expects
RUN useradd -m -s /bin/bash default-user && \
    echo "default-user ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers

# Create test mount directories
RUN mkdir -p /appdata /media/tv /media/movies

VOLUME ["/sys/fs/cgroup"]

CMD ["/lib/systemd/systemd"]
